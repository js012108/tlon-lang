importar mas.__init__

funcion mult()
    x = MeasurementAgent(1, "someRandomText", 20)
    x.start()
end

funcion stringD()
    p = OneStringDetector("", 1, "xxx")
    p.start()
    retorno(p)
end

#sleep(5)
#zzz = stringD()

mult()
